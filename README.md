# WaterMassClassification
Objective water mass classification using different dimensionality reduction methods.
These include no dimensionality reduction, Principal Component Analysis (PCA or EOF), Autoencoder (AE) and Variational Autoencoder (VAE)
on scaled and unscaled data.